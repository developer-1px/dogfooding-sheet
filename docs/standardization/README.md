# Standardization Registry

This directory tracks package candidates that may become `@interactive-os/*`
standards. App-internal workspace packages do not belong here unless they are
being evaluated for public interactive-os promotion.

| Candidate | Status | Source | Target package |
|---|---|---|---|
| Formula engine | Candidate | `src/lib/formula` | `@interactive-os/formula-engine` |

